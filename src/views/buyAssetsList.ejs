<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/styles.css">
    <script src="javascripts/javascript.js"></script>
</head>

<body>
    <nav class="navbar">
        <div class="logo">MyAsset</div>

        <div class="menu-toggle" id="mobile-menu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>

        <ul class="nav-list">
            <li><a href="/">Trang chủ</a></li>
            <li><a href="/buyAssetsList">Danh sách mua</a></li>
        </ul>
    </nav>
    <h1>Buy Asset</h1>
    <div>
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Ngày Mua</th>
                    <th scope="col">Loại Tài Sản</th>
                    <th scope="col">Số Lượng</th>
                    <th scope="col"></th>

                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i < data.length; i++){ %>
                    <tr>
                        <th scope="row" class="align-middle">
                            <%=i +1 %>
                        </th>
                        <td class="align-middle">
                            <%=new Date(data[i].ngaymua).toLocaleDateString('vi-VN')%>
                        </td>
                        <td class="align-middle">
                            <%=data[i].tenloaitaisan %>
                                <%= data[i].tentaisan %>
                        </td>
                        <td class="align-middle">
                            <%= Number(data[i].soluong)%>
                        </td>
                        <td>
                            <a href="/getDetailBuyList/<%=data[i].id%>">Xem chi tiết</a>
                        </td>
                    </tr>
                    <%}%>

            </tbody>
        </table>


    </div>
</body>

</html>